<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:jms="http://www.springframework.org/schema/jms"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		 http://www.springframework.org/schema/jms http://www.springframework.org/schema/jms/spring-jms.xsd">

    <description>收报配置</description>
    
    <!-- Message converter -->
    <bean id="simpleMessageConverter" class="org.springframework.jms.support.converter.SimpleMessageConverter" />

	<!-- 定义Queue监听器(用来接收字符串) -->
	<jms:listener-container destination-type="queue" container-type="default" 
		connection-factory="connectionFactory" acknowledge="auto" message-converter="simpleMessageConverter">
		<jms:listener destination="sampleQueue" ref="sampleStringAction"/>
	</jms:listener-container>
	
	<!-- 定义Topic监听器 (用来接收字符串)-->
	<jms:listener-container destination-type="topic" container-type="default" 
		connection-factory="connectionFactory" acknowledge="auto" message-converter="simpleMessageConverter">
		<jms:listener destination="sampleTopic" ref="sampleStringAction"/>
	</jms:listener-container>
	
	<!-- 定义Queue监听器(用来接收序列化的 Java对象) -->
	<jms:listener-container destination-type="queue" container-type="default" 
		connection-factory="connectionFactory" acknowledge="auto" message-converter="simpleMessageConverter">
		<jms:listener destination="sampleQueue" ref="sampleObjectAction"/>
	</jms:listener-container>
	
	<!-- 定义Topic监听器 (用来接收序列化的 Java对象)-->
	<jms:listener-container destination-type="topic" container-type="default" 
		connection-factory="connectionFactory" acknowledge="auto" message-converter="simpleMessageConverter">
		<jms:listener destination="sampleTopic" ref="sampleObjectAction"/>
	</jms:listener-container>
	
	<bean id="sampleQueue" class="org.apache.activemq.command.ActiveMQQueue">
        <constructor-arg index="0" value="${jms.queue.somnus.sample}" />
    </bean>
    
    <bean id="sampleTopic" class="org.apache.activemq.command.ActiveMQTopic">
        <constructor-arg index="0" value="${jms.topic.somnus.sample}" />
    </bean>

</beans>